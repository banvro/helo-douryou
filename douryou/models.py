# from django.db import models
# from django.contrib.auth.models import AbstractUser, AbstractBaseUser
# from douryou.manager import UserManager
# # Create your models here.
# # class MyUser(AbstractBaseUser):
# #     phone_number = models.CharField(max_length=40, unique=True)
 
# #     USERNAME_FIELD = 'phone_number'

# class User(AbstractUser):
#        # username = models.AutoField(primary_key=True)
#        phone_number = models.CharField(max_length=120, primary_key=True)
#        is_seller = models.BooleanField(default=False)
#        is_user = models.BooleanField(default=False)
#        code = models.CharField(max_length=20, default=1234)

#        USERNAME_FIELD = 'phone_number'
#        REQUIRED_FIELDS = []
#        objects = UserManager()
       
#        # def __str__(self):
#        #        return self.username + phone_number
# # USERNAME_FIELD = 'phone_number'












# from django.db import models
# from django.contrib.auth.models import AbstractUser, AbstractBaseUser
# from douryou.manager import UserManager
# # Create your models here.
# # class MyUser(AbstractBaseUser):
# #     phone_number = models.CharField(max_length=40, unique=True)
 
# #     USERNAME_FIELD = 'phone_number'

# class User(AbstractUser):
#        # username = models.AutoField(primary_key=True)
#        phone_number = models.CharField(max_length=120, primary_key=True)
#        is_seller = models.BooleanField(default=False)
#        is_user = models.BooleanField(default=False)
#        code = models.CharField(max_length=20, default=1234)

#        USERNAME_FIELD = 'phone_number'
#        REQUIRED_FIELDS = []
#        objects = UserManager()
       
#        # def __str__(self):
#        #        return self.username + phone_number
# # USERNAME_FIELD = 'phone_number'